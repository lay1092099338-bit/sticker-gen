# 2026-04-21 Daily Notes

## Sticker Gen — Production Deploy
- User asked for latest prod URL; server was down (PM2 had no processes)
- Restarted prod sticker-gen on port 7788 with cloudflared tunnel
- **Merged dev → master** (9 commits including history feature) and deployed to production
- Prod URL: `https://andale-requesting-duke-pdt.trycloudflare.com` (cloudflared quick tunnel, changes on restart)
- History records, click-to-swap element, differentiated variant strategies — all now in prod

## Sticker Gen — Banner Feature (NEW, dev branch, test env)
- **User request**: Add "横幅" (banner) mode — 300×50cm banners for Amazon retail, alongside existing circular stickers
- Implemented: top tab bar (🔵 圆形贴纸 / 🟣 横幅), mode switching with purple theme for banners
- History records separated by `itemType: sticker | banner`
- Banner prompt: Amazon party banner style — gradient backgrounds, large centered text, decorative elements (diamonds, ribbons, glitter, confetti), ornate borders, commercial print quality
- 5 variants same as stickers: 2 faithful similar, 1 subtle variation, 1 new color scheme, 1 creative exploration
- First iteration had display issues (strip-item too small to see banner). Fixed: 600px wide items, 6:1 aspect ratio, object-fit:contain
- Prompt was too generic initially. Rewrote based on user's reference images (Happy 60 Birthday style banners with diamond borders, glitter, ribbons)
- **Test env**: port 7789, tunnel URL `https://tries-streets-strikes-straight.trycloudflare.com`
- Status: deployed to test, user testing in progress. Not yet merged to master/prod.

## Infrastructure Notes
- PM2 processes: `sticker-default` (port 7788, prod), `sticker-dev` (port 7789, test), `sticker-tunnel` (prod cloudflared), `sticker-dev-tunnel` (test cloudflared)
- Cloudflared quick tunnels generate random URLs on each restart
- Git: sticker-gen repo on `dev` branch for banner work
