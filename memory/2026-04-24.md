# 2026-04-24 日志

## 贴纸生成器项目（sticker-gen）

### 项目位置
- 代码：`/home/ubuntu-m/.openclaw/workspace-lay-a/sticker-gen/`
- 测试地址：`https://tries-streets-strikes-straight.trycloudflare.com`
- Dev 服务：port 3001（test env），port 3000（prod env）

### 今日工作

#### 1. 新增水瓶贴纸 Tab（bottle 模式）
- 顶部新增第三个 tab【💧 水瓶贴纸】，蓝色主题
- 尺寸：23×6cm，aspect_ratio 用 `4:1`（16:4）
- 复用横幅宽图生成函数 `callModelverseBanner`，传 `mode='bottle'`
- 新增 `buildBottlePrompt` 函数，风格偏商用生日贴纸
- 历史记录、AI 编辑、换元素、Prompt 优化等功能全自动适配
- commit: `feat: 新增水瓶贴纸tab(23x6cm, bottle模式), 复用宽图生成逻辑, 蓝色主题, 独立prompt风格`

#### 2. 历史记录加载性能优化（大文件 base64 剥离）
**问题根因：** prod `history_prod.json` 达 512MB，每次读历史都要全量读文件

**解决方案：** 把图片 base64 从 JSON 剥离，单独存为 `history/images/xxx.jpg` 文件
- 新增 `/api/history-img/:filename` 接口 serve 图片文件
- `saveHistory` 改为存 `imageUrl: /api/history-img/xxx.jpg`
- 前端兼容 `imageUrl || imageDataUrl` 两种字段
- 写了迁移脚本 `migrate-history.js`，跑完后：
  - prod JSON：512 MB → 64 KB
  - test JSON：27 MB → 4 KB
  - 图片文件存在 `history/images/` 目录

**注意：** `sendFile` 需要 `root: '/'` 才能用绝对路径，否则 404

#### 3. 水瓶贴纸 Prompt 优化（三段式布局）
根据用户提供的参考图总结的布局规范，写入 `buildBottlePrompt`：

**三段式布局（固定结构）：**
- 左段：中等图案 + 小字 + sparkle 散点
- 中段（hero）：字更大 + 椭圆/菱形金属描边框 + 更华丽主图 + 金属光泽字
- 右段：左段镜像/呼应

**五种强制点缀（每张必须有）：**
1. glitter drip — 顶部金/银粉往下飘
2. sparkles — 全宽随机星光点
3. confetti — 小碎片散落
4. 水平珠光边框线 — 顶底各一条装饰线
5. 渐变背景 — 中间亮、两侧深

风格和颜色完全跟着用户上传的参考图走，不写死，适配不同 Excel 内容

### 项目背景
- 三种模式：`sticker`（圆形贴纸）、`banner`（横幅）、`bottle`（水瓶贴纸）
- 后端：Node.js/Express，用 Modelverse API 生成图片
- 历史文件：`history/history_test.json`、`history/history_prod.json`
- 图片文件：`history/images/`
