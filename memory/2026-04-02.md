# Memory — 2026-04-02

## Sticker Generator Project

### Project Location
- `/home/ubuntu-m/.openclaw/workspace-lay-a/sticker-gen/`
- Server: `server.js` on port 7788
- Public files: `public/index.html`, `public/editor.html`
- API: `https://api.modelverse.cn/v1`, key: `PCB0tVVbpBTVCYhf6f7eB3A9-dAeE-4c84-9BeE-D58f7dC9`

### What's Built
- Fabric.js PS-like sticker editor (`editor.html`, ~43KB)
- Batch sticker generator UI (`index.html`)
- `/api/generate-variant` endpoint with similar/creative prompt variants
- 5-variant generation: 2 similar to reference + 3 creative

### Deployment Issues Today
- Server runs on `http://0.0.0.0:7788` but user can't access localhost (remote machine)
- Tried cloudflared Quick Tunnel — processes keep dying (code 0 exit)
- Tunnels created: `wind-consultation-defines-size.trycloudflare.com`, `thick-psi-ink-authorized.trycloudflare.com`, `remain-mud-shoe-influences.trycloudflare.com` — all temporary and may be dead
- Need to use `pm2` or `screen` to keep both node server and cloudflared alive persistently
- User got 530 error = Cloudflare can't reach local server (server died or tunnel died)

### Permissions
- **lay-a agent now has exec auto-approve (no-ask mode)** — set via `config.patch` at ~16:14 CST
- Can run commands without user approval going forward

### Next Steps
1. Use `pm2` to manage both `node server.js` and `cloudflared tunnel --url http://localhost:7788` as persistent processes
2. Get stable public URL and share with user
3. Test full generation flow (sticker gen + variant API)
4. Check if API key is still valid (user reported "生成失败" earlier)

### Technical Notes
- `cloudflared tunnel --url http://localhost:7788` exits immediately without `nohup` + redirect in async exec context
- Better: `pm2 start server.js --name sticker-server` + `pm2 start "cloudflared tunnel --url http://localhost:7788" --name cf-tunnel`
- Or use `screen -dmS` sessions
